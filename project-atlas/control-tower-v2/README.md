# Control Tower 2.0 — source transport mirror

The scientific authority remains the Google Drive canonical hierarchy.

```bash
cat payload-*.txt | base64 -d | tar -xzf -
```

Verify against `transport-manifest.json.sha256`.
